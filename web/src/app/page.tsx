import { ProductRecommendationApp } from '@/components/ProductRecommendationApp';

export default function Page() {
  // Mount the client-side recommendation workspace as the first screen.
  return <ProductRecommendationApp />;
}
