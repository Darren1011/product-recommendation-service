{
  "name": "product-recommendation",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for a JSON-backed product recommendation prototype.",
  "scripts": {
    "install:services": "npm --prefix services run install",
    "install:web": "npm --prefix web install",
    "dev:services": "npm --prefix services run dev",
    "dev:web": "npm --prefix web run dev",
    "test:services": "npm --prefix services run test",
    "tsc:web": "npm --prefix web run tsc",
    "build:web": "npm --prefix web run build"
  }
}
